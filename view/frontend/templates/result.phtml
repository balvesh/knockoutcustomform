
<table class="data table" id="customform-data-table">
    <caption class="table-caption">Form Data</caption>
    <thead>
        <tr>
            <th scope="col" class="col name">Name</th>
            <th scope="col" class="col email">Email</th>
            <th scope="col" class="col phone">Phone</th>
            <th scope="col" class="col message">Message</th>
            <th scope="col" class="col created_at">Created At</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($block->getFormData() as $key => $data): ?>
            <tr>
                <td data-th="Name" class="col name"><?= $data->getName(); ?></td>
                <td data-th="Email" class="col email"><?=$data->getEmail(); ?></td>
                <td data-th="Phone" class="col phone"><?=$data->getPhone(); ?></td>
                <td data-th="Message" class="col message"><?=$data->getMessage(); ?></td>
                <td data-th="Date" class="col created_at"><?php echo $data->getCreatedAt(); ?></td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>